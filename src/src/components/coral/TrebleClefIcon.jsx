export default function TrebleClefIcon({ className = '', ...props }) {
  return (
    <span
      aria-hidden="true"
      className={`inline-flex items-center justify-center font-serif leading-none ${className}`}
      {...props}
    >
      𝄞
    </span>
  );
}
